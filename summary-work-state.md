# PICC — Session Work State

## Objective
Finish the owner-approved PICC plan in sequence. G3 COMPLETE; G4 VERIFIED COMPLETE. G5 = Pack 1 "Local Trading Core" (`docs/specs/PICC_PACK1_LOCAL_TRADING_CORE_v1.md`) — S0–S5 COMPLETE + live verified. **S6 T6.2 session-capture kill-switch feature COMPLETE + code-reviewed (2026-09-15):** PICC Settings toggle + extension popup toggle + AND-precedence + pending-handoff coercion, full suite 2177 green. **Owner directive (2026-09-15): EO session capture (S6 remaining: re-login → all-four-running evidence → human observation rows) is DEFERRED — "we focus on expertoption session capture afterwards, now proceed with the remaining stuff as planned."** Remaining non-EO plan items: `POST /api/webfetch/limits/reset` live check (DONE 2026-09-15), doc sync. Cactus Needle T0 runtime remains DEPENDENCY-NOT-YET-AVAILABLE by design (no code ships without the runtime; honest skip tested).

## Important Details
- **Honesty contract (test-enforced, spec risk 1/5):** `observed: null` renders `"not-observed"` never 0; observed 0 stays 0; `stopped-at-human` exits ONLY via human ack; exact SKIP_REASONS strings; unobserved usage (null) never blocks; news digest items/summaries NEVER invented; captcha gates DETECTED and reported per source, never silently bypassed.
- **S0 envelope (final):** Pack 1 = `pack1-local-trading-core` (id NOT "pack1"): p1-1 EO capture l-class/T0/30min, p1-2 CCXT poll run/T1/15s/rpm60, p1-3 news digest run/T3/10min/rpm6, p1-4 signals run/T1/45s/rpm0. Registry via `localStore("pack_registry")` (D8, durable).
- **S3 env surface (in `.env.example` lines 39-52):** `PICC_NEWS_FEEDS`, `PICC_WEBFETCH_MAX_RPM` (30/min/host), `PICC_WEBFETCH_SOLVER` (empty = never engage), `PICC_NEWS_DIGEST_SYNTHESIS`, `PICC_NEWS_DIGEST_MAX_ROWS` (200), **`PICC_NEWS_DIGEST_MAX_ITEMS` (new — default 50, live 200)**. No `rateLimited(` may appear in packObservers/packRunner/packRegistry (T2.2) — webfetch/newsDigest are separate files.
- **Owner answers (2026-09-14) — Q1/Q5/Q6 decided, Q2 governance model, Q3/Q4 in-progress:**
  - **Q1 — MASTER SWITCH: OK.** `PICC_CAPTURE_MASTER` (server env + Settings toggle, config-pushed to extension) so web-app-capture-off ⇒ extension-capture-off; extension-off never stops web app. No such switch exists in code yet.
  - **Q2 — GOVERNANCE MODEL (recorded, affects settings design):** every suite/ministry gets its own dedicated capture settings; FOR NOW the whole trading suite owns "the trading suite" settings. Features/agents/models/systems/resources may be ministry-specific OR shared. **Prime Minister = intelligence suite (DSS/ES): full authority over ALL of PICC's resource pool**, higher level than other suites; lower ministries auto-adhere (auto-regulate) to higher-ups' decisions. Resource pool = web/globe of interlinked resources of any kind, regulated/managed in the intelligence suite; mapping respects TIER (lower-tier resource defers to higher-tier). Ministries configured with own + shared resources; PM manages every resource including ministry-specific. → Settings page hierarchy: PM-level (PICC Settings) ≥ ministry-level (per-suite tabs); capture master sits at PM level.
  - **Q3 — extension vs web-app independence: UNDER REVIEW.** Owner offered two directions: (a) improvise everything in the extension (usable standalone without web app; likewise disabled) or (b) remove the independency of either presence when both exist, forcing them to work together. **Recommendation GIVEN (see research below): keep independent presence, synergize STATE + SETTINGS; never force co-presence.** Grounded in existing designed architecture: `docs/specs/PICC_INCOME_GENERALIZATION_requirements_v1.md` REQ-H ("Extension alone ... Nothing in the extension REQUIRES the web UI ... nothing in the web UI REQUIRES the extension") + `PICC_MULTISOURCE_ENGINE.md` REQ-6 (no-webapp viability: sensor queues with backoff, queue cap 400, popup 3 honesty states online/offline/standby) + `PICC_INCOME_GENERALIZATION_design_v1.md` independence rule (`content.js:520-527` built-in fallback = extension-alone; web-app-alone via GET `/api/connectors/:slug/collect`). Forced coupling would break already-shipped no-webapp viability and the owner's own "extension disabled ≠ web app stops" answer. Synergy = single registry + settings cascade via the existing `capture-profiles` channel + `sourceLeg` attribution.
  - **Q4 — heartbeat "additional context": the existing docs answer it.** `PICC_PACK1_LOCAL_TRADING_CORE_v1.md:34` — extension IS the primary capture leg (content.js:313-365): relays ONLY on token change (in-memory dedup `captureSent`), kill-switch `piccSessionCapture` (chrome.storage, defaults ON), heartbeat flush every 30s. `EXTENSION_CONNECTIVITY_ENGINE.md` — heartbeat cadence corrected (worker 30s alarm; sensor re-probes /api/health every 15s), ingest 240/60s, heartbeat 30/60s. → captureState field should carry what the extension ALREADY tracks: `{killSwitch: piccSessionCapture, queueDepth (cap 400), venues:{expertoption:{onPage, loggedIn, capturable, captureSentSinceHeartbeat}}}` — bounded, no page content. Awaiting owner confirmation this is the context meant.
  - **Q5 — ITEM CAP: RAISE IT. DONE.** `digestBudgetFromEnv(env)` new export (default 50 → `PICC_NEWS_DIGEST_MAX_ITEMS` override; garbage falls back honestly), scheduler passes the env budget (+4 tests). `.env` = 200, `.env.example` documented. **Live verified: items 50→200, 5 sources contributing per pass (forexlive 25 + cointelegraph 30 + coindesk 25 + google-news 102 + yahoo 18), beincrypto still captcha-gated, full suite 206/2127 green, typecheck 0.**
  - **Q6 — per-source posture: KEEP gated/failing feeds in the list** (honest per-pass counts; no fallback chain). beincrypto stays (gate auto-heals if lifted). No action needed.
  - **Q7 — captcha: research DONE + proposal.** Options found: self-hosted OSS (Botright GPL-3.0 — reCAPTCHA 50-80%/hCaptcha ≤90%/GeeTest sliders, Python/Playwright; CapSnap MIT pure-Python OCR for text CAPTCHAs; OhMyCaptcha MIT 867★ self-hostable YesCaptcha-API solver w/ local vision model; CaptchaKraken needs GPU vLLM — too heavy) and paid APIs (2Captcha/CapSolver/NopeCHA ~$3/1000). **Proposal: `PICC_WEBFETCH_SOLVER=none|notify|<future adapter>` — default `none` (pure detection to keep honesty); `notify` = owner's manual path: captcha-gated source raises an in-app notification with a "solve in mini-window" action (summon minified window/iframe loading the gated URL for the OWNER to solve), then webfetch retries with the session's cookies. Human-solved, observed, no silent automation — consistent with PATHWAY/stopped-at-human. Remote/local solver adapters later, explicit opt-in + honesty label.
- **Feed set (live, persisted in `.env`):** 21 URLs / 20 hosts (9 S3 + 12 added: decrypt, theblock, newsbtc, u.today, cryptopotato, beincrypto⚠gated, bitcoinmagazine, cnbc×2, bbc-business, fxstreet, investing.com). 20 of 21 probe-verified; beincrypto captcha-gated at webfetch layer (kept intentionally). Server proven to boot feeds purely from `.env` (no shell env).
- **S1 seam facts:** `headlessSessionStatus()` (captureProfiles.mjs:447) → per-venue rows incl. `sourceLeg`; `liveEOStats()` (liveEO.mjs:1086) → status + sticky degraded; extension kill-switch `piccSessionCapture` CLIENT-SIDE (content.js chrome.storage), heartbeat (handlers.mjs:5005-5029) stores only version/installTime/activeTab/cookieCount — the seam for `captureState` (scope per Q4 above).
- **Cactus Needle T0 runtime = DEPENDENCY-NOT-YET-AVAILABLE** → T1.2 honest skip; `PICC_CACTUS_T0_RUNTIME === "available"` flips it.
- **Dev-stack quirks:** no hot-reload — the vite dev server caches ALL server module state at boot; edits to `server/*` require a FULL restart (kill by PID/port, relaunch `scripts/dev-restart.mjs` — a detached spawner that survives tool timeouts: writes `logs/vite-dev.{out,err}.log` + `logs/vite-dev.pid`). `ChildProcess.kill` wrapper error on multi-statement Start-Process is spurious — processes DO launch; PS 5.1 UTF-8 mojibake in probes (verify with Read tool before "fixing"); stale node orphans hold 5173/8000 — kill by port (Get-NetTCPConnection) before restart. **Live server 2026-09-15 = PID 12708** (boot 13:26 local = 05:26Z, binds `::`) — started AFTER all review-fix mtimes (packObservers.mjs 11:27 / sessionCaptureSettings.mjs 11:58 / packRegistry.mjs 12:00), so the review-fix code IS live (verified by Get-Process StartTime vs file mtimes + webfetch `sinceReset` 05:26:39Z matching boot). **PID 1284 (pre-fix, boot 11:20) was replaced by that restart.** **Stale orphan PID 20564 (10:02, pre-coerce code, bound `::1:5173`) KILLED ~12:18** — it was the second writer in the `pack_registry.json` ENOENT rename race; after the kill: err log static across a full 60s tick (0 bytes delta), registry file mtime tracks every tick, single listener on 5173. No restart is needed for the review-fix code — only the EO re-login (deferred) requires the owner's browser actions.
- **EO session capture root cause (2026-09-14, live-diagnosed):** the p1-1 CONTEXT_ONLY_FOR_REAL_USER failure was a two-layer bug. (1) **Sticky degraded**: `softReconnectLiveEO()` never cleared `degraded`/`lastError` before reconnecting — every fresh token + restart left `degraded={kind:"expired"}` set, so every 60s observation re-stuck p1-1 to stopped-at-human (8 owner acks all failed identically). FIX: softReconnect clears both before `startLiveEO()` (liveEO.mjs ~1017, regression-tested). (2) **Wiring crash**: once degraded cleared, the observer honestly mapped p1-1 to `running`, but a stopped-at-human step can only exit via human ack (legal map) → `observeStep` threw → the WHOLE pack-observation tick died every 60s (err log: `illegal transition stopped-at-human -> running`), starving p1-2/3/4 and freezing the registry. FIX: `coerceObservationForStoppedStep()` in packObservers.mjs — the scheduler rewrites a running-intent on a stopped step to a SAME-STATUS stopped-at-human observation (legal, records fresh evidence "seam healthy; step awaits human ack", never auto-resumes); the registry legal map is UNTOUCHED (direct runStep still rejects, pinned by tests).
- **Session-capture kill-switch feature (S6/T6.2, owner decision 2026-09-15, ADR-0001):** AND semantics — PICC settings OFF ⇒ no capture even if extension toggle ON (`session-capture-disabled`, `capturePathway` need:"capture" surfaces the Settings card); PICC unset + extension OFF ⇒ no capture (`extension-capture-disabled`); both enabled/unset ⇒ capture. Null is NEVER assumed OFF (default-ON, honesty contract). Surfaces: server store+routes (`/api/settings/session-capture` GET public/POST auth-gated, folded `sessionCaptureEnabled` into capture-profiles — the extension's server view), Settings.tsx card (instant-post toggle, honest "never changed" note when unconfigured), popup switch (disabled + note when the server view is OFF), content.js gate (returns before local check when the server observed OFF). **Pending-handoff coercion:** a kill-switch skip on a `stopped-at-human` step is rewritten same-status (never cancels the handoff; prior pathway + observed facts preserved; coerce takes `priorPathway` from the step projection). Registry persistence proven live (641 rows on disk; the earlier `localStore write failed pack_registry: ENOENT rename` was a two-server race — root-caused and eliminated when orphan PID 20564 was killed 2026-09-15 ~12:18; single writer since, every tick persists).
- **S6 code review (fixed point HEAD adabd71, 2026-09-15):** two axes (Standards + Spec, parallel sub-agents). Fixed: (1) `serverSessionCaptureEnabled` in content.js never reset — a server-observed OFF outlived server silence; now `null` on any pass without a fresh boolean (ADR independence restored; absent never treated as OFF); (2) sessionCaptureSettings store dropped its fragile mtime/Date.now() cache (read-per-call, ~30-byte file, privacy-critical kill must reflect on-disk truth); (3) `ackStep` evidence row now carries `doneBy:"human"` (spec T6.2 literal field, pinned); (4) popup relay/capture toggle handlers deduped via `flipToggle(key)`; (5) dead `token` param dropped from public `getPackRegistry`. Accepted judgement calls (noted, not changed): S4 `unconfiguredChannelReason` cascade duplication + 4× runStep couplet (pre-existing, outside slice); heartbeat-spoof surface (the route is intentionally unauthenticated per extension architecture, bounded by 30/min IP limit — auth change is a design decision for the owner); registry evidence rows use `{ts,status,detail,observed,doneBy}` rather than the spec's minimal `{kind,by}` shape (richer vocabulary pinned by tests).
- **Live diagnosis artifacts:** the running server (16:45 local) predated the liveEO fix (17:26 local) → the 8 failed acks ran OLD code. Registry evidence proved it: 276 rows, 2 running (08:36-08:37Z), transition at 08:38Z, same CONTEXT reason re-reported every 60s through 10:12Z — the sticky degraded, not fresh failures. After restart with BOTH fixes: health `status:"idle"` (was "expired"), registry updatedAt live (10:53Z), p1-1 stopped-at-human with honest "seam healthy; step awaits human ack" evidence, p1-2/3/4 running with fresh rows, ZERO new scheduler errors (err log size static over 70s).

## Work State
### Completed
- **G3 COMPLETE; G4 verified complete** (no manufactured work).
- **G5 S0–S3 COMPLETE + live verified** — packRegistry/packRunner/registry-API (26 tests), observeEoCapture+t0 sub-step (12), observeCcxtPoll (4), webfetch (23), newsDigest (24), webfetch API (5), scheduler observation + 10-min RUN leg (stagger 90s). Spec S3 execution notes appended.
- **Full suite green @ S3:** 206 files / 2123 tests exit 0; typecheck 0.
- **Feed expansion + live verification (2026-09-14):** probed 19 candidates, 13 verified, 6 honestly rejected; `.env` → 21 feeds; restart with NO shell env → boot from `.env`; pass `feeds:21 ok:20 gated:1 items:50`; read-only probe identified the gate (beincrypto, captcha-gated) and the item-cap mechanism. `.env` comment corrected honestly.
- **Digest cap raise (Q5, this slice):** `digestBudgetFromEnv` + scheduler wiring + 4 tests (env default 50; 200 override; garbage→50; 3-source 60-item fixture proves default saturates one source at 50 vs raised cap collects all 180). `.env=200`, `.env.example` documented. **Live verified: store row 06:40:47Z `feeds=21 ok=20 gated=1 rateLimited=0 items=200`; probe per-source: forexlive 25, cointelegraph 30, coindesk 25, google-news 102, yahoo 18.** Full suite re-run: **206 files / 2127 tests passed (4 new)**; typecheck exit 0.
- **Extension research read (Q3/Q4 grounding):** EXTENSION_CONNECTIVITY_ENGINE.md, PICC_MULTISOURCE_ENGINE.md (REQ-6, T7 server-status), PICC_INCOME_GENERALIZATION_{design,requirements,checklist}_v1.md (REQ-H independence, built-in fallback), PACK1 spec :34, PICC_SUITE_MINISTRY_MODEL_v1.md (governance), HEADLESS_CAPTURE_ENGINE (REQ-D).
- **Captcha research done (Q7):** OSS solvers + paid APIs + manual-mini-window proposal (none|notify|adapter).
- **S4 COMPLETE + live verified (this slice):** `observeSignalNotifications` in `packObservers.mjs` (PURE mapping from notifier seam) + 7 tests (engine-disabled skip, null→default-ON, channel-row mapping from config alone incl. no-vapid/no-webhook-url, no-dispatch-yet, failed channel w/ reason, volume count, user-disabled channel) → 31 tests in file. Scheduler p1-4 wired (9th dynamic import `notifier.mjs`); live registry: `p1-4-signal-notifications: running | signal engine active (last dispatch 2026-08-28T09:55:24.412Z)`, `pack-observation lastOk=True`.
- **notifier loadState migration fix (surfaced by S4 live wiring):** persisted `notifications.json` predated the T4 `snoozes` key → `notifierStatus()` crashed `Object.keys(state.snoozes)` ("Cannot convert undefined or null to object") every 60s tick, killing pack-observation after p1-1/2/3. `loadState()` now merges defaults UNDER parsed (preserves user prefs, normalizes collections) + 3 regression tests (`notifierStateMigration.test.mjs`): status survives + snoozes 0, legacy `email` channel preserved + `webhook` default merged, `snoozeAlert` mutation path works. Full suite: **207 files / 2137 tests passed, 0 failures**; typecheck exit 0.
- **S5 COMPLETE + live verified (this slice):** two spec-implied server gaps closed first (no `POST /api/packs/ack` route — `ackStep` was test-only; T5.2 footer needs server caps since the browser can't read envs). Server: added `POST /api/packs/ack` (rate-limited 10/60s, auth-gated via `requireAuth`, 400 on non-stopped/unknown — the ONLY legal exit from stopped-at-human over HTTP) + `caps: resourceCaps()` in GET `/api/packs/registry` payload (+7 tests in `packRegistryApi.test.mjs`). Client: `src/lib/packRegistry.ts` (types + `getPackRegistry`/`ackPackStep`/`envelopeFactLine`), `src/components/PackRegistryStrip.tsx` (polls 30s — under the 30/60s read limit — with "as of" timestamp, per-step status badge + envelope fact line, L-class "needs human: re-login" chip, ack button ONLY on stopped-at-human, honest "registry unreachable" on failure, §8.5 caps footer + `PICC_RESOURCE_*` config note, no settings affordance; after ack it re-reads from the server, never fabricates the re-arm), mounted in `MarketsRoom.tsx` above the panels (+5 tsx tests, jsdom/flushSync pattern). **Live probed:** caps 4096/50/2048 in GET payload; POST ack on the live stopped-at-human p1-1 → `ok=True status=idle acknowledgedBy=human`; next scheduler tick honestly re-stopped it (`stopped-at-human · needs: re-login`, evidence 211 rows) since the EO token is still expired. Full suite: **208 files / 2149 tests passed**; typecheck exit 0.
- **Two-bug p1-1 stabilization (2026-09-14):** (1) `softReconnectLiveEO()` now clears `degraded=null; lastError=null` before `startLiveEO()` — the stale-degraded loop that swallowed 8 owner acks is broken. (2) New `coerceObservationForStoppedStep()` pure function in `packObservers.mjs` rewrites a `running`-intent to legal `same-status stopped-at-human` at the scheduler wiring seam — the pack-observation tick no longer crashes when the seam is healthy but the step is awaiting human ack; registry unfreezes; p1-2/3/4 no longer starved. Both fixes live-verified (208 files / 2154 tests green, err log static over 70s+ across ticks). **Full suite after pathway landing: 208 files / 2157 tests passed, typecheck exit 0.**
- **S6 T6.1 T6.2 partial (pathway + persistence proof + env kill-switch, 2026-09-14):** Server projects `loginPathway` from latest evidence row onto registry read surface (`projectReadStep` in packRegistry.mjs — never fabricated; null unless observer recorded one). `PackStep.pathway: PackPathway | null` typed in client. Strip renders structured prompt + ordered steps + ack button on stopped-at-human L-class steps with pathway present (+1 api test, +1 registry test, +1 tsx test; **full suite 208 files / 2157 tests**). Persistence proven: server restart at 12:40Z → token read from vault (encrypted pva1 envelope `trading-credentials.json`) → liveEO reconnected at 12:41Z without owner action → EO gateway rejected at 12:42Z (demo lifecycle, external to PICC). **Env kill-switch observed live + reverted** (PICC_SIGNAL_ENGINE=0 → p1-4 skip-unconfigured; =1 → running). **Extension kill-switch relay shipped** (background.js heartbeat carries captureEnabled; handler tri-state; scheduler passes it; handler test; extensionIntegrity storage pin 7→8 — the new call reads the boolean preference, never a credential). Full suite after relay: **208 files / 2158 tests, typecheck exit 0**. Remaining S6: extension-reload live observation of captureEnabled=false, final evidence-row recording at `/suites/trading`.
- **S6 T6.2 session-capture kill-switch feature COMPLETE (2026-09-15):** full slice — server store `sessionCaptureSettings.mjs` (default-ON, boolean-only, no cache) + `/api/settings/session-capture` GET/POST (auth-gated POST) + `sessionCaptureEnabled` folded into capture-profiles; `observeEoCapture` settings gate first (`session-capture-disabled` + `capturePathway` need:"capture") then extension gate (`extension-capture-disabled`); `SKIP_REASONS.sessionCaptureDisabled`; Settings.tsx "Session capture" card (instant-post, honest unconfigured note); extension popup `#capture` switch (disabled + note when server view OFF) + `#capture-note`; content.js `serverSessionCaptureEnabled` gate (reset-to-null fix); PackRegistryStrip renders `need:"capture"` pathway on skipped steps (no ack button there); ADR-0001 + ADR index. **Live verified 2026-09-15:** settings default ON/unconfigured → POST false (configured) → p1-1 stayed `stopped-at-human` with kill-switch detail + `sessionCaptureEnabled:false` evidence + pathway need:"capture" → RESTORED to ON → p1-1 honest `needs: re-login` again. **Coerce fix for kill-switch skips on stopped steps** (same-status rewrite, priorPathway from projection, handoff never auto-cancelled; 3 new tests). **Code review fixes applied** (content.js null-reset, store cache removal, `doneBy:"human"` ack evidence, flipToggle dedup, dead token dropped). Full suite: **210 files / 2177 tests passed, typecheck exit 0**.

### Active
- **Owner directive 2026-09-15: EO session capture DEFERRED** — "we focus on expertoption session capture afterwards, now proceed with the remaining stuff as planned." The S6 final run (EO re-login → all-four-running → human observation rows) moves to a dedicated later pass; the durable-fix steps below stay documented for it.
- **`POST /api/webfetch/limits/reset` live check DONE (2026-09-15):** on PID 12708 — GET `/api/webfetch/limits` (30/60s) returned all observed hosts `inWindow:0`; POST reset returned `{ok:true, limits, stats}` with `sinceReset` bumped `05:26:39.735Z → 06:40:02.106Z` and host stats honestly cleared (observedHosts 0 until the next fetch). **Auth nuance recorded honestly:** `requireAuth` (handlers.mjs:5117) treats localhost requests as authenticated (`isLocalhostRequest`), so from this dev machine the reset is reachable without a Bearer token; for non-localhost clients the `verifyUser` Bearer check still applies (same pattern as POST /api/packs/ack + capture-config). Unit tests remain the authority for the non-localhost 401 path (webfetchApi.test.mjs).
- **S6 close-out (owner-in-the-loop) — 2026-09-15 ~13:45 LOCAL, ROOT-CAUSED:** the session-capture kill-switch feature is COMPLETE and code-reviewed. **What happened since 13:30:** (1) your move-2 extension re-login WORKED and was HEALTHY (vault write 13:22:48, old server observed `seam healthy` at 05:25:05Z — EO accepted the extension-relayed token); (2) the agent's server restart (PID 1284 → 12708) respawned the studio browser on its PERSISTENT profile (`launchPersistentContext`), which restored a STALE EO demo session; (3) the studio pass at 05:27:25Z saw a token DIFFERENT from the vault → `tokenChanged:true` → overwrote your good token → `restartLiveEO` → EO rejected 2s later (`ERROR_CONTEXT_ONLY_FOR_REAL_USER`, the recurring demo-lifecycle rejection); (4) p1-1 is now honestly `stopped-at-human needs: re-login` (`tokenConfigured:true`, `tokenChangedAt:05:27:25Z`, `sourceLeg:studio`), and the system is in stable-stale equilibrium — the studio pass re-ran 05:57:25Z with NO tokenChanged (vault already equals the stale token) so nothing re-clobbers on its own. **MITIGATION APPLIED (documented T7 seam):** `POST /api/trading/capture-config { expertoption: { refreshCadenceMs: 86400000 } }` — live + persisted to `capture-config.json`; the studio leg cannot re-clobber for 24h; the EXTENSION leg is NOT cadence-gated so it still works; metrics cadence untouched (5 min). **OWNER NEXT ACTIONS (durable fix — DEFERRED to the dedicated EO-capture pass):** (1) LOG OUT of EO in the STUDIO browser window (it's a REAL headed window on this machine, `PICC_STUDIO_HEADLESS` unset; the stale demo session lives there) — or close that EO tab — so the stale token source is gone for good; (2) re-login to the EO DEMO account in your OWN browser with the PICC extension per the pathway steps (keep the tab open 60s for the relay); (3) ack the handoff in the packs strip → p1-1 re-arms → all-four-running → record the final evidence. Blocker remains EXTERNAL (EO gateway rejecting demo-context tokens reused by automation), now confined to a single stale studio session instead of a recurring clobber.

### Blocked
- p1-1 live `stopped-at-human needs: re-login`: needs REAL EO demo login via extension capture leg (owner's choice; plain-browser re-login does NOT propagate a token — proven). S6 all-four-running blocked on this — **DEFERRED by owner to the dedicated EO-capture pass (2026-09-15).**
- Cactus Needle T0 runtime NOT shipped — DEPENDENCY-NOT-YET-AVAILABLE by design (spec non-goal; `PICC_CACTUS_T0_RUNTIME === "available"` flips the T1.2 sub-step, honest skip tested). Firecrawl CLI unusable keyless (external tool, not in repo).
- ~~POST /api/webfetch/limits/reset live check needed a session~~ — **DONE 2026-09-15 (see Active).**

## Next Move
1. Proceed with ANY remaining non-EO plan items the owner names; otherwise the plan's remaining work is the deferred EO pass itself.
2. When the owner returns to EO capture (the dedicated pass): (a) log out of EO in the STUDIO browser window or close that EO tab (headed window, `PICC_STUDIO_HEADLESS` unset); (b) re-login to the EO DEMO account in the OWN browser with the PICC extension per the pathway steps (keep the tab open 60s for the relay); (c) ack the handoff in the packs strip → p1-1 re-arms → all-four-running evidence at `/suites/trading`; (d) record final `{kind:"observation", by:"human"}` evidence rows per S6. Server is ALREADY on review-fix code (PID 12708) — no restart needed.
3. Keep `.env` (GITIGNORED) + `summary-work-state.md` in sync.

## Relevant Files
- `docs/specs/PICC_PACK1_LOCAL_TRADING_CORE_v1.md` (§S3 notes appended; :34 extension-is-primary-capture-leg; §S4/T4.1-2, §S5/T5.1-2, §S6).
- `apps/dashboard/.env` (GITIGNORED): 21 feeds + `PICC_NEWS_DIGEST_MAX_ITEMS=200` + owner's VAPID keys + `PICC_SIGNAL_ENGINE=1`; proven boot-source.
- `apps/dashboard/.env.example`: S3 env surface incl. new `PICC_NEWS_DIGEST_MAX_ITEMS` (lines ~39-52).
- `apps/dashboard/server/services/newsDigest.mjs` — NEW `digestBudgetFromEnv`; `newsFeedsConfig/parseFeedXml/runDigest/storeDigestRun/digestSynthesis`.
- `apps/dashboard/server/__tests__/newsDigest.test.mjs` — +4 digestBudgetFromEnv tests (28→32 in file).
- `apps/dashboard/server/services/scheduler.mjs` — news-digest RUN leg passes `budget: digestBudgetFromEnv()`.
- `apps/dashboard/server/services/webfetch.mjs` + `newsDigest.mjs`, `packObservers.mjs` (**S4 `observeSignalNotifications`**), `packRunner.mjs`/`packRegistry.mjs`, `handlers.mjs` (registry; webfetch limits; extension heartbeat 5005-5029 seam), `scheduler.mjs` (**p1-4 wired — 9th dynamic import `notifier.mjs`**).
- `apps/dashboard/server/services/notifier.mjs` — **loadState migration for pre-T4 persisted shapes** (defaults merged under parsed); `notifierStatus()` is the p1-4 seam (`recent` cap 20 + `channels`).
- `apps/dashboard/server/__tests__/notifierStateMigration.test.mjs` — NEW: 3 regression tests (status survives legacy shape, prefs preserved + default channels merged, snoozeAlert on migrated state).
- `apps/dashboard/server/__tests__/packObservers.test.mjs` — 34 tests (24 S0-S5 + 7 S4 + 3 coerce-fix: kill-switch skip on stopped step preserves status/facts/pathway; extension variant; non-kill-switch pass-through).
- **Session-capture kill-switch (S6/T6.2, this slice):** `apps/dashboard/server/services/sessionCaptureSettings.mjs` (NEW — default-ON store, boolean-only, read-per-call no cache) + `server/__tests__/sessionCaptureSettings.test.mjs` + `sessionCaptureSettingsApi.test.mjs`; `handlers.mjs` — GET/POST `/api/settings/session-capture` + `sessionCaptureEnabled` folded into capture-profiles; `packObservers.mjs` — settings gate first (`session-capture-disabled`, `capturePathway` need:"capture") + `coerceObservationForStoppedStep(observation, currentStatus, priorPathway)`; `scheduler.mjs` passes `sessionCaptureEnabled()` + `currentStep?.pathway`; `packRegistry.mjs` — `doneBy:"human"` ack evidence row + `need:"capture"` pathway type; `src/lib/api.ts` `SessionCaptureSettingsView`/`getSessionCaptureSettings`/`saveSessionCaptureSettings`; `src/pages/Settings.tsx` Session capture card + `Settings.test.tsx`; `src/lib/packRegistry.ts` (dead token dropped); `src/components/PackRegistryStrip.tsx` `showPathway` gate + capture-pathway test; extension popup.html `#capture`+`#capture-note`, popup.js `renderSessionCapture` + `flipToggle`, background.js forwards `sessionCaptureEnabled`, content.js `serverSessionCaptureEnabled` gate (reset-to-null on server silence); `captureContracts.test.mjs` locks (mirror incl. `piccSessionCapture`, TOGGLES=2, probes=3); `docs/adr/README.md` + `docs/adr/0001-session-capture-kill-switch-precedence.md` (NEW, accepted).
- `apps/dashboard/src/lib/packRegistry.ts` — NEW (S5): typed client; **`PackPathway` interface added (T6.2)**.
- `apps/dashboard/src/components/PackRegistryStrip.tsx` — NEW (S5): polls `/api/packs/registry` 30s, honest unreachable, status badges + envelope lines, needs-human chip + ack (only stopped-at-human), §8.5 caps footer; **pathway block renders structured prompt + ordered steps (T6.2)**.
- `apps/dashboard/src/components/__tests__/PackRegistryStrip.test.tsx` — NEW (S5): 5 jsdom tests; **+1 pathway render test (T6.2) = 6**.
- `apps/dashboard/server/services/packRegistry.mjs` — **`projectReadStep()` (T6.2)**: pathway from latest evidence's `observed.pathway`, applied in getRegistry/getStep/observeStep/ackStep.
- `apps/dashboard/extensions/picc-overlay/background.js` — **kill-switch relay (T6.2): heartbeat body carries `captureEnabled` from chrome.storage `piccSessionCapture` (boolean only, absent = null)**.
- `apps/dashboard/server/handlers.mjs` — **heartbeat stores boolean-only tri-state `captureEnabled`**; NEW `POST /api/packs/ack` (rate-limited 10/60s, auth-gated); GET `/api/packs/registry` now carries `caps: resourceCaps()`.
- `apps/dashboard/server/services/scheduler.mjs` — **passes `globalThis.__picc_ext_heartbeat?.captureEnabled ?? null` into observeEoCapture (T6.2)**.
- `apps/dashboard/server/__tests__/packRegistryApi.test.mjs` — **+2 tests: pathway projection on stopped step; kill-switch relay tri-state + skip mapping (14 total)**.
- `apps/dashboard/src/pages/ministry/MarketsRoom.tsx` — strip mounted above the panels.
- `apps/dashboard/server/__tests__/packRegistry.test.mjs` — **+1 pathway projection test (10 total)**.
- `apps/dashboard/server/__tests__/extensionIntegrity.test.mjs` — **T13 pin 7→8 storage calls: added read is the boolean kill-switch preference, never a credential**.
- Extension seams: `apps/dashboard/extensions/picc-overlay/content.js` (313-365 venue session obs, `captureSent` dedup, `piccSessionCapture` kill-switch; 520-527 built-in fallback), `background.js` (30s heartbeat alarm — **now relays captureEnabled**; server-status probe), `popup.js` (3-state honesty), `handlers.mjs` ext-heartbeat 30/60s (+ tri-state `captureEnabled`) + ext-ingest 240/60s.
- Governance model: `docs/specs/PICC_SUITE_MINISTRY_MODEL_v1.md`.
- Capture master (Q1, to build): none exists — proposed `PICC_CAPTURE_MASTER` env + Settings toggle + config push via `capture-profiles` channel.